name: terminal
kind: terminal.service
meta:
  comment: "Default terminal service."
  depends_on:
    - "btea.terminal"

target: "btea.terminal"
hide_logs: true

lifecycle:
  auto_start: true
  start_timeout: 5s
  restart:
    initial_delay: 1s
    max_attempts: 3