name: endpoints.websocket_demo
kind: http.endpoint
meta:
  server: web_server
  router: api_router
  comment: "WebSocket demo with echo server."
  depends_on:
    - functions.websocket_demo
    - web_server
    - api_router

# Endpoint configuration
method: GET
path: /ws-demo
target: functions.websocket_demo
json_output: true