version: 1.0/beta

name: web_server
kind: http.service
meta:
  comment: "Default http service."
  groups: [ "system" ]

addr: ":8082"
timeouts:
  idle: 60s

lifecycle:
  auto_start: true
  restart:
    initial_delay: 5s
    max_attempts: 3