name: web_server
kind: http.service
meta:
  comment: "Default http service."
  groups: [ "system" ]

addr: ":8081"
timeouts:
  idle: 60s

lifecycle:
  auto_start: true
  restart:
    initial_delay: 5s
    max_attempts: 3