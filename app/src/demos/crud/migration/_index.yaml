version: "1.0"
namespace: app.migrations

meta:
  depends_on: [ "ns:wippy.lib" ]

entries:
  - name: user_table
    kind: migration.lua
    meta:
      description: "Create users table with indexes"
      db_namespace: "app"
      db_types: [ "sqlite" ]
      tags: [ "schema", "users", "init" ]
      timestamp: "2025-03-07T12:00:00Z"
    source: file://user_table_migration.lua
    method: define_migration
    imports:
      migration: "wippy.lib:migration"