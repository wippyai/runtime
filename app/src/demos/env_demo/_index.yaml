version: "1.0"
namespace: demo.envs

meta:
  depends_on: [ ns:system ] // TODO
  description: "ENV demo implementation"

entries:
  # JSON API for security operations
  - name: api
    kind: function.lua
    meta:
      comment: "Env demo implementation"
      env:
        - name: openai_api_key
          envstorage: memenvstorage
          readonly: true
        - name: openai_api_url
          envstorage: memenvstorage
          readonly: true
    source: file://api.lua
    method: handler
    modules: [ envstorage ] # // TODO rename to env
