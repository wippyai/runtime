version: "1.0"
namespace: app.demo.todo_list

meta:
  depends_on: [ "ns:wippy.lib" ]
  db_namespace: "todo_list"
  db_types: [ "sqlite" ]
  type: "migration"

entries:
  - name: todo_table
    kind: function.lua
    meta:
      description: "Create todo_items table with indexes"
      timestamp: "2025-03-07T12:00:00Z"
    source: file://add_todo_table.lua
    method: migrate
    imports:
      migration: "wippy.lib:migration"
    pool:
      size: 1