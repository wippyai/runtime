version: "1.0"
namespace: app.demo.todo_list

meta:
  depends_on: [ "ns:wippy.migration" ]
  type: "migration"
  target_db: system:db

entries:
  - name: todo_table
    kind: function.lua
    meta:
      description: "Create todo_items table with indexes"
      timestamp: "2025-03-07T12:00:00Z"
    source: file://add_todo_table.lua
    method: migrate
    imports:
      migration: "wippy.migration:migration"
    pool:
      size: 1