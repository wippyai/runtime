version: "1.0"
namespace: app.activity

entries:
  - name: process_data
    kind: function.lua
    meta:
      comment: "Simple data processing function"
      temporal_activity:
        name: "ProcessData"
        task_queue: "system:simple_queue" #todO: make auto register work!
      #depends_on:
      #  - system:simple_queue
    source: file://process_data.lua
    method: handler
    modules: [ "json" ]
    pool:
      size: 2