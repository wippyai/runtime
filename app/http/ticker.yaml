name: endpoints.ticker
kind: http.endpoint
meta:
  server: web_server
  router: api_router
  comment: "Ticks time."
  depends_on:
    - function.ticker
    - web_server
    - api_router

# Endpoint configuration
method: GET
path: /ticker
target: function.ticker
json_output: true