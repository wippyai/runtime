name: demo_workflow
kind: temporal.workflow_binding
meta:
  comment: "Our default workflow."
  depends_on: [ "default_tq", "simple.workflow" ]
  depends_on_groups: [ "system" ]

task_queue: "default_tq"
workflow: "simple.workflow"