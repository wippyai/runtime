```lua
{
    -- Core parameters
    model = "claude-3-7-sonnet-20250219",  
    messages = messages, -- as generated by prompt library               
    
    -- Thinking configuration 
    thinking_effort = 0, -- 0 - 100, provider and model specific
    
    -- Tool configuration (for tool calling only)
    tool_ids = {"system:weather", "tools:calculator"},
    tool_schemas = {
        -- manually defined tools in a format of tool_resolver
    },                     
    tool_call = "auto", -- auto, `tool-name` (forced), singular (forbids multicalls)    
    
    -- Streaming configuration
    stream = {             -- for streamer
        reply_to = "process-id",               
        topic = "llm_response",                
    },
    
    -- Generation parameters (optional and model specific)
    options = {
        temperature = 0.7,
        top_p = 0.9,
        top_k = 40,
        max_tokens = 1024,
        -- provider specific
    }
}
```