name: pony_tests

on:
  push:
    branches:
      - main
      - develop
  pull_request:

jobs:
  tests:
    name: Wippy Tests (Go ${{ matrix.go }}, OS ${{matrix.os}})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix:
        go: [1.24]
        os: ["ubuntu-latest", "windows-latest"]
    steps:
      - name: Check out code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT_TOKEN_IGOR }}  # Use PAT with repo scope for cross-org access

      - name: Setup msys
        if: matrix.os == 'windows-latest'
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: mingw-w64-x86_64-toolchain mingw-w64-x86_64-sqlite3
          path-type: inherit

      - name: Set up Go ${{ matrix.go }}
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go }}

      - name: Configure Git and Go for private modules
        env:
          GITHUB_TOKEN: ${{ secrets.PAT_TOKEN_IGOR }}
        run: |
          # Configure Git to use token from environment
          git config --global url."https://${GITHUB_TOKEN}@github.com/".insteadOf "https://github.com/"
          
          # Configure Go to skip verification for private modules
          go env -w GOPRIVATE=github.com/ponyruntime/*,github.com/wippyai/*
          go env -w GONOSUMDB=github.com/ponyruntime/*,github.com/wippyai/*
          go env -w GONOPROXY=github.com/ponyruntime/*,github.com/wippyai/*

      - name: Install Go dependencies
        run: |
          go mod download
          
      - name: Verify Go environment
        run: |
          echo "GOPRIVATE: $(go env GOPRIVATE)"
          echo "GONOSUMDB: $(go env GONOSUMDB)"
          echo "GONOPROXY: $(go env GONOPROXY)"

      - name: Run golang tests
        if: matrix.os != 'windows-latest'
        env:
          CGO_ENABLED: 1
          GORACE: "halt_on_error=1"
        run: |
          # Run tests with parallel execution and shorter timeouts
          go test --tags "fts5 sqlite_vec" -race -timeout=10m -parallel=4 ./...

      - name: Run golang tests
        if: matrix.os == 'windows-latest'
        env:
          CGO_ENABLED: 1
          GORACE: "halt_on_error=1"
        run: |
          # Run tests with parallel execution and shorter timeouts
          go test --tags "fts5 sqlite_vec" -race -timeout=10m -parallel=4 ./...
        shell: msys2 {0}
