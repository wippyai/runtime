name: pony_tests

on:
  push:
    branches:
      - main
      - develop
  pull_request:

jobs:
  tests:
    name: Wippy Tests (Go ${{ matrix.go }}, OS ${{matrix.os}})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    strategy:
      matrix:
        go: [1.24]
        os: ["ubuntu-latest", "windows-latest"]
    steps:
      - name: Check out code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}  # Use Personal Access Token

      - name: Setup msys
        if: matrix.os == 'windows-latest'
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: mingw-w64-x86_64-toolchain mingw-w64-x86_64-sqlite3
          path-type: inherit

      - name: Set up Go ${{ matrix.go }}
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go }}

      - name: Configure Git and Go for private modules
        run: |
          # Configure Git credentials for private repos
          git config --global credential.helper store
          echo "https://${{ secrets.GITHUB_TOKEN }}:x-oauth-basic@github.com" > ~/.git-credentials
          chmod 600 ~/.git-credentials
          
          # Configure Go to skip verification for private modules
          go env -w GOPRIVATE=github.com/ponyruntime/*
          go env -w GONOSUMDB=github.com/ponyruntime/*
          go env -w GONOPROXY=github.com/ponyruntime/*

      - name: Install Go dependencies
        run: |
          go mod download

      - name: Run golang tests
        if: matrix.os != 'windows-latest'
        env:
          CGO_ENABLED: 1
        run: |
          go test --tags "fts5 sqlite_vec" -race ./...

      - name: Run golang tests
        if: matrix.os == 'windows-latest'
        env:
          CGO_ENABLED: 1
        run: |
          go test --tags "fts5 sqlite_vec" -race ./...
        shell: msys2 {0}
