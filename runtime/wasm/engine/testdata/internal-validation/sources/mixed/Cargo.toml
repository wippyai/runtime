[package]
# This manifest is a regeneration scaffold, not a recovered original manifest.
name = "mixed"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "=0.58.0"

[profile.release]
opt-level = 2
panic = "abort"
