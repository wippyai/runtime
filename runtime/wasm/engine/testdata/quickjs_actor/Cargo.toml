[package]
name = "quickjs-actor-guest"
version = "0.1.0"
edition = "2021"
license = "MPL-2.0"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
rquickjs = "=0.12.2"
wit-bindgen = "=0.58.0"

[profile.release]
opt-level = 2
panic = "abort"
strip = "symbols"
