name: terminal
kind: terminal.service
meta:
  comment: "Default terminal service."
  depends_on:
    - "counter.terminal"
    - "snake.terminal"
    - "basic.terminal"

target: "chat.terminal"

lifecycle:
  auto_start: true
  start_timeout: 5s
  restart:
    initial_delay: 1s
    max_attempts: 3