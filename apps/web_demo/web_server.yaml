name: service.web_server
kind: http.server
meta:
  comment: "Default http server."

# Server configuration
listen: ":8080"
http:
  idle_timeout: 60s

lifecycle:
  auto_start: true
  restart:
    delay: 5s
    max_attempts: 3