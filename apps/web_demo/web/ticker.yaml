name: endpoints.ticker
kind: http.endpoint
meta:
  server: service.web_server
  router: service.api_router
  comment: "Ticks time."
  depends_on:
    - functions.ticker
    - service.web_server
    - service.api_router

# Endpoint configuration
method: GET
path: /ticker
target: functions.ticker
json_output: true