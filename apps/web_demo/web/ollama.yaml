name: endpoints.ollama
kind: http.endpoint
meta:
  server: web_server
  router: api_router
  comment: "Returns the ollama response."
  depends_on:
    - functions.http_time
    - web_server
    - api_router

# Endpoint configuration
method: GET
path: /ollama
target: functions.ollama