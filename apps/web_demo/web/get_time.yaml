name: endpoints.get_time
kind: http.endpoint
meta:
  server: service.web_server
  comment: "Returns the current time."
  depends_on:
    - functions.http_time
    - service.web_server

# Endpoint configuration
method: GET
path: /time
target: functions.http_time