version: "1.0"
namespace: app.users.relay

meta:
  depends_on: [ "ns:app", "ns:app.user.auth" ]
  comment: "User WebSocket Relay System"

entries:
  - name: ws_endpoint
    kind: function.lua
    meta:
      comment: "WebSocket authentication and join endpoint"
    source: file://ws_endpoint.lua
    method: handler
    modules: [ http, security, json ]

  - name: ws_endpoint.endpoint
    kind: http.endpoint
    meta:
      comment: "WebSocket join endpoint"
      router: app:api
    method: GET
    path: /ws/join
    func: ws_endpoint

  - name: central.service
    kind: process.service
    meta:
      comment: "Central Hub Service"
    process: central
    host: app:processes
    lifecycle:
      auto_start: true

  - name: central
    kind: process.lua
    meta:
      comment: "Process that manages central users relay"
    source: file://central.lua
    method: run
    modules: [ time, json ]
    imports:
      actor: "wippy.actor:actor"

  - name: user
    kind: process.lua
    meta:
      comment: "Process for individual user WebSocket connections pool"
      default_host: app:processes
    source: file://user.lua
    method: run
    modules: [ time, json, funcs, uuid ]
    imports:
      actor: "wippy.actor:actor"